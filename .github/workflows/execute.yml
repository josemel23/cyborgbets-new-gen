name: Test Con Inputs

on:
  workflow_dispatch:
    inputs:
      accion:
        required: true
        type: choice
        options:
          - scraper
          - deploy

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test con parametros
        run: |
          echo "Accion recibida: ${{ github.event.inputs.accion }}"
