name: Panel Scraper
on:
  workflow_dispatch:
    inputs:
      accion:
        required: true
        type: choice
        options:
          - scraper
          - deploy
      liga:
        required: false
        type: string
        default: liga_mx

jobs:
  scraper:
    runs-on: ubuntu-latest
    if: inputs.accion == 'scraper'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install Chrome
        run: |
          sudo apt-get update
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
      - name: Setup Display
        run: |
          export DISPLAY=:99
          Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &
          sleep 3
          echo "DISPLAY=:99" >> $GITHUB_ENV
      - name: Install Python deps
        run: |
          pip install undetected-chromedriver selenium pandas requests beautifulsoup4
      - name: Run scraper
        run: |
          cd "scraper/${{ inputs.liga }}"
          timeout 600 python3 "${{ inputs.liga }}_standings.py"
      - name: Move files to correct location
        run: |
          echo "📁 Moviendo archivos a la ubicación correcta..."
          mkdir -p public/static
          if [ -f "scraper/${{ inputs.liga }}/public/static/${{ inputs.liga }}_data.json" ]; then
            echo "✅ Moviendo ${{ inputs.liga }}_data.json"
            cp "scraper/${{ inputs.liga }}/public/static/${{ inputs.liga }}_data.json" "public/static/"
            echo "Archivo movido a: public/static/${{ inputs.liga }}_data.json"
          fi
          find "scraper/${{ inputs.liga }}/" -name "*.json" -exec echo "JSON encontrado: {}" \;
          find "scraper/${{ inputs.liga }}/" -name "*.json" -exec cp {} "public/static/" \;
          echo "=== Archivos en public/static/ ==="
          ls -la public/static/ || echo "Carpeta public/static/ vacía"
      - name: Commit files
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add .
          if ! git diff --cached --quiet; then
            git commit -m "Auto-update: ${{ inputs.liga }}"
            git push
          fi

  deploy:
    runs-on: ubuntu-latest
    if: inputs.accion == 'deploy'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Detect project type and setup
        id: setup
        run: |
          echo "🔍 Detectando tipo de proyecto..."
          
          if [ -f "package.json" ]; then
            echo "📦 Proyecto Node.js/React detectado"
            echo "project_type=nodejs" >> $GITHUB_OUTPUT
            echo "PROJECT_TYPE=nodejs" >> $GITHUB_ENV
          elif [ -f "requirements.txt" ] && [ -f "app.py" ]; then
            echo "🐍 Proyecto Python/Flask detectado"
            echo "project_type=flask" >> $GITHUB_OUTPUT
            echo "PROJECT_TYPE=flask" >> $GITHUB_ENV
          elif [ -f "requirements.txt" ] && [ -f "main.py" ]; then
            echo "🐍 Proyecto Python general detectado"
            echo "project_type=python" >> $GITHUB_OUTPUT
            echo "PROJECT_TYPE=python" >> $GITHUB_ENV
          elif [ -f "index.html" ]; then
            echo "🌐 Proyecto HTML estático detectado"
            echo "project_type=static" >> $GITHUB_OUTPUT
            echo "PROJECT_TYPE=static" >> $GITHUB_ENV
          else
            echo "❓ Tipo de proyecto no detectado, asumiendo estático"
            echo "project_type=static" >> $GITHUB_OUTPUT
            echo "PROJECT_TYPE=static" >> $GITHUB_ENV
          fi

      # OPCIÓN 1: Deploy a GitHub Pages (para proyectos estáticos)
      - name: Deploy to GitHub Pages
        if: steps.setup.outputs.project_type == 'static'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          force_orphan: true

      # OPCIÓN 2: Deploy a Vercel (para Node.js/React/Static)
      - name: Setup Node.js for Vercel
        if: steps.setup.outputs.project_type == 'nodejs'
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install and Build for Vercel
        if: steps.setup.outputs.project_type == 'nodejs'
        run: |
          npm install
          npm run build || echo "No build script, usando archivos actuales"
          
      - name: Deploy to Vercel
        if: steps.setup.outputs.project_type == 'nodejs' || steps.setup.outputs.project_type == 'static'
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-args: '--prod'
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
          working-directory: ./

      # OPCIÓN 3: Deploy a Netlify (alternativa para estáticos)
      - name: Deploy to Netlify
        if: false  # Cambia a true si quieres usar Netlify en lugar de las otras opciones
        uses: nwtgck/actions-netlify@v2.0
        with:
          publish-dir: './public'
          production-branch: main
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      # OPCIÓN 4: Deploy a Railway - MÉTODO OFICIAL
      - name: Setup Node.js for Railway CLI
        if: steps.setup.outputs.project_type == 'flask' || steps.setup.outputs.project_type == 'python'
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Deploy to Railway
        if: steps.setup.outputs.project_type == 'flask' || steps.setup.outputs.project_type == 'python'
        run: |
          echo "🚂 Iniciando deploy a Railway..."
          
          # Instalar Railway CLI
          npm install -g @railway/cli@latest
          
          # Verificar instalación
          railway --version
          
          # Conectar a Railway usando el token
          railway login --browserless
          
          # Listar proyectos (para debug)
          railway projects
          
          # Deploy del proyecto
          if [ -n "${{ secrets.RAILWAY_SERVICE }}" ]; then
            echo "🎯 Deploying a servicio específico: ${{ secrets.RAILWAY_SERVICE }}"
            railway up --service ${{ secrets.RAILWAY_SERVICE }}
          else
            echo "🎯 Deploying a proyecto por defecto"
            railway up
          fi
          
          echo "✅ Deploy a Railway completado"
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

      # OPCIÓN 5: Deploy a Heroku (alternativa confiable)
      - name: Deploy to Heroku
        if: false  # Cambia a true si quieres usar Heroku
        uses: akhileshns/heroku-deploy@v3.12.14
        with:
          heroku_api_key: ${{ secrets.HEROKU_API_KEY }}
          heroku_app_name: ${{ secrets.HEROKU_APP_NAME }}
          heroku_email: ${{ secrets.HEROKU_EMAIL }}
          usedocker: false

      # OPCIÓN 6: Deploy a Render (alternativa moderna)
      - name: Deploy to Render
        if: false  # Cambia a true si quieres usar Render
        run: |
          echo "🎨 Iniciando deploy a Render..."
          
          # Trigger deploy usando Render API
          curl -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys" \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{}'
          
          echo "✅ Deploy a Render iniciado"

      # OPCIÓN 7: Deploy a PythonAnywhere (específico para Python)
      - name: Deploy to PythonAnywhere
        if: false  # Cambia a true si quieres usar PythonAnywhere
        run: |
          echo "🐍 Deploying a PythonAnywhere..."
          
          # Subir archivos via SFTP/rsync
          sudo apt-get install -y sshpass rsync
          
          # Sync archivos
          sshpass -p "${{ secrets.PYTHONANYWHERE_PASSWORD }}" \
            rsync -avz --delete ./ \
            ${{ secrets.PYTHONANYWHERE_USER }}@ssh.pythonanywhere.com:/home/${{ secrets.PYTHONANYWHERE_USER }}/mysite/
          
          echo "✅ Archivos sincronizados a PythonAnywhere"

      - name: Deploy Success Notification
        run: |
          echo "✅ Deploy completado exitosamente!"
          echo "🚀 Proyecto tipo: ${{ steps.setup.outputs.project_type }}"
          echo "📅 Fecha: $(date)"
          echo "🌐 Tu aplicación debería estar disponible en línea"
