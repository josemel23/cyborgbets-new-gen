name: Test Simple

on:
  workflow_dispatch:
    inputs:
      accion:
        required: true
        type: choice
        options:
          - scraper
          - deploy
      liga:
        required: false
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          echo "Funcionando desde GitHub Actions"
          echo "Accion: ${{ inputs.accion }}"
          echo "Liga: ${{ inputs.liga }}"
